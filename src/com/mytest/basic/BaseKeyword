1、native 关键字：调用本地的操作系统函数
   eg：private native void start0();  //依赖于不同的操作系统实现，即使用操作系统提供的底层函数来进行处理

JNI（Java Native Interface）技术：使用操作系统提供的底层函数进行特殊处理

2、synchronized 关键字：同步机制，在同步代码块里只允许一个线程执行，不允许多个线程同时修改同一个变量
  同步块：  对于同步块，synchronized 获取的是参数中的对象锁
  synchronized(obj){  //obj 同步对象：当线程执行到这里的同步块时，必须获取 obj 这个对象的锁才能执行同步块，否则线程只能等待获得锁
      //同步代码操作
  }

3、volatile 关键字：主要在属性上使用，无法描述同步处理，只是一种属性直接内存的处理，避免一些其他处理地方的拷贝处理
   在线程中，与同步 synchronized 一同使用

4、transient 关键字：在属性上使用，在序列化对象中，防止某些属性被序列化。
   当进行反序列对象读取时，该关键字修饰的属性的值为属性数据类型的默认值

5、Annotation 关键字：注解（Annotation）又称 Java 标注，是 JDK 1.5 引入的一种注释机制，Java 标注可以通过反射获取标注内容
